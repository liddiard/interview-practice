function rotate90(image) {
  var tmp;
  // iterate over half of image rows
  for (var i = 0; i < image.length / 2; i++) {
    for (var j = 0; j < image[i].length; j++) {
      tmp = image[j]
    }
  }
}
